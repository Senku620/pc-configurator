# 🖥️ ПК Конфигуратор - Telegram Mini App

Telegram Mini App для сборки конфигураций ПК с красивым темным дизайном.
Над проектом ведутся работы, только начал разработку так что пока очень сыро

![Preview](https://img.shields.io/badge/Platform-Telegram-blue) ![Python](https://img.shields.io/badge/Python-3.10+-green) ![License](https://img.shields.io/badge/License-MIT-purple)

## ✨ Возможности

- 🎨 Премиальный темный дизайн (Atlas-inspired)
- 📱 Полностью адаптивен для мобильных устройств
- 🔧 6 категорий компонентов (CPU, GPU, RAM, Storage, PSU, Case)
- 💰 Автоматический расчет стоимости
- 📊 Оценка производительности сборки
- 📤 Поделиться конфигурацией
- 💾 Сохранение сборок
- 🤖 Интеграция с Telegram Bot API

## 🚀 Быстрый старт

### Шаг 1: Клонирование и установка

```bash
# Клонируй репозиторий (или скачай файлы)
cd "Конфигуратор засекречено"

# Установи зависимости Python
pip install -r requirements.txt
```

### Шаг 2: Запуск локального сервера

```bash
python server.py
```

Открой в браузере: http://localhost:8080

## 📁 Структура проекта

```
Конфигуратор засекречено/
├── static/
│   └── index.html      # Mini App (HTML + CSS + JS)
├── data/
│   └── builds.json     # Сохраненные сборки
├── server.py           # Python HTTP сервер
├── bot.py              # Telegram Bot
├── requirements.txt    # Python зависимости
├── .env.example        # Пример переменных окружения
├── .gitignore
└── README.md
```

## 🤖 Настройка Telegram Bot

### 1. Создай бота

1. Открой [@BotFather](https://t.me/botfather) в Telegram
2. Отправь `/newbot`
3. Введи имя бота (например: "ПК Конфигуратор")
4. Введи username (например: `pc_configurator_bot`)
5. Скопируй токен бота

### 2. Настрой Mini App

1. В @BotFather отправь `/mybots`
2. Выбери своего бота
3. Нажми **Bot Settings** → **Menu Button** → **Configure menu button**
4. Введи URL твоего Mini App
5. Введи текст кнопки: "🖥️ Открыть"

### 3. Запусти бота

```bash
# Windows
set BOT_TOKEN=your_token_here
set WEBAPP_URL=https://your-domain.com
python bot.py

# Linux/Mac
export BOT_TOKEN=your_token_here
export WEBAPP_URL=https://your-domain.com
python bot.py
```

## 🌐 Деплой

### Вариант 1: Ngrok (для тестирования)

```bash
# Запусти сервер
python server.py

# В другом терминале запусти ngrok
ngrok http 8080
```

Используй полученный URL (https://xxx.ngrok.io) как WEBAPP_URL

### Вариант 2: GitHub Pages (бесплатно)

1. Создай репозиторий на GitHub
2. Загрузи содержимое папки `static/` в репозиторий
3. Включи GitHub Pages в настройках репозитория
4. URL будет: `https://username.github.io/repo-name/`

### Вариант 3: Vercel / Netlify

1. Подключи репозиторий к Vercel/Netlify
2. Укажи `static` как директорию для публикации
3. Получи URL и используй его как WEBAPP_URL

### Вариант 4: VPS сервер

```bash
# На сервере
git clone <your-repo>
cd "Конфигуратор засекречено"
pip install -r requirements.txt

# Запуск с помощью systemd или screen
screen -S pc-config
python server.py
# Ctrl+A, D - чтобы отключиться от screen
```

## 🛠️ API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Mini App (index.html) |
| GET | `/api/health` | Health check |
| GET | `/api/builds` | Список сохраненных сборок |
| POST | `/api/save-build` | Сохранить новую сборку |

### Пример запроса сохранения сборки:

```bash
curl -X POST http://localhost:8080/api/save-build \
  -H "Content-Type: application/json" \
  -d '{
    "components": {
      "cpu": {"id": "intel-i7-13700k", "name": "Intel Core i7-13700K", "price": 35000},
      "gpu": {"id": "rtx-4070", "name": "NVIDIA RTX 4070", "price": 55000}
    },
    "totalPrice": 90000,
    "perfScore": 92
  }'
```

## 🎨 Кастомизация

### Цветовая схема

Цвета настраиваются в CSS переменных в `static/index.html`:

```css
:root {
    --color-bg-primary: #0f0f0f;      /* Основной фон */
    --color-bg-secondary: #1a1a1a;    /* Фон карточек */
    --color-accent-purple: #7c3aed;   /* Акцентный цвет */
    --color-accent-blue: #6366f1;     /* Второй акцент */
    --color-text-primary: #ffffff;    /* Основной текст */
    --color-text-secondary: #d1d5db;  /* Вторичный текст */
}
```

### Добавление компонентов

Компоненты настраиваются в JavaScript секции `static/index.html`:

```javascript
const components = {
    cpu: [
        { id: 'new-cpu', name: 'Новый процессор', specs: 'Описание', price: 50000, perf: 95 },
        // ...
    ],
    // другие категории...
};
```

## 📝 Лицензия

MIT License - используй как хочешь!

## 🤝 Контакты

Если есть вопросы - создай Issue или напиши в Telegram ![Uploading image.png…]()
!

---

**Made with 💜 for Telegram Mini Apps**
